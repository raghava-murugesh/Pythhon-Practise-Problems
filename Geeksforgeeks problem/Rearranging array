Problem -> https://www.geeksforgeeks.org/problems/rearranging-array1648/1?page=1&difficulty=Basic&sortBy=difficulty

Solution :
#User function Template for python3

class Solution:    
    def Rearrange(self, arr):
        #Code Here
        arr.sort()
        result = []
        
        i = 0
        j = len(arr) - 1
        
        while i <= j:
            if i == j:
                result.append(arr[i])
                
            else:
                result.append(arr[i])
                result.append(arr[j])
                
            i += 1
            j -= 1
                
        return result
